- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.vimrc: vim/.vimrc
    ~/.vim_runtime: vim/.vim_runtime
    ~/.tmux.conf: tmux/.tmux.conf
    ~/.tmux: tmux/.tmux
    ~/.config/fish:
      force: true
      path: fish
