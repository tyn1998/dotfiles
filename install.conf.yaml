- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.vimrc: vim/.vimrc
    ~/.vim: vim/.vim
    ~/.tmux.conf: tmux/.tmux.conf
    ~/.tmux: tmux/.tmux
    ~/.config/fish:
      force: true
      path: fish
